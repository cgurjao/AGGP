import networkx as nx
import numpy as np
import matplotlib as np


# The number of the genes we want
nb_gene = 10

# to create the random matrix for the initialisation
genome = np.reshape(np.random.random_integers(0,1,size=n**2),(n,n))
# print genome

